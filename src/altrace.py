




class Game(DirectObject):

  def __init__(self):


      def movealongtrack
          #control position (xyz)
    # trackpath = Mopath.Mopath()
    # trackpath.loadFile('../models/testtrack4path')
    # trackInterval = MopathInterval(trackpath, self.worldNP, duration=30, name = 'track')
    # self.trackpath = Sequence(trackInterval)
    # self.trackpath.start()

            control rotation

            Mopath.fFaceForward = False

            m = loader.loadModel('../models/trackcurve')
            m.ls()
